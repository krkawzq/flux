# Copy this to .flux/.env and fill in values for env interpolation in westlake.yml.
# .flux/.env is auto-loaded by flux at config load time (.flux/ is gitignored).
#
# Override the test password:
WESTLAKE_PASSWORD=your_real_password_here

# If you put any other ${VAR} references in westlake.yml, declare them here.
