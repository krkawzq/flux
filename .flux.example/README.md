# `.flux.example`

这是仓库内的配置示例模板，用来展示 `.flux/` 的目录结构与配置形状，不包含真实 secret。

使用方式：

1. 复制 `.flux.example/` 为 `.flux/`
2. 按需填入你自己的真实配置值
3. 检查 `.flux/` 中的 token、密码、OAuth 凭证与主机信息是否都已替换

说明：

- `.flux/` 已在仓库根 `.gitignore` 中忽略，用于存放本地真实配置
- `.flux.example/` 应始终保持脱敏状态，只保留结构、字段和示例占位符
